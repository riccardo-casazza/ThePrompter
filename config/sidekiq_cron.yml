import_orchestrator:
  cron: "0 3 * * *"  # Every day at 3:00 AM
  class: "ImportOrchestratorJob"
  description: "Import IMDb data, Plex library, and consolidate TMDB mappings"

tmdb_refresh:
  cron: "0 * * * *"  # Every hour at minute 0
  class: "TmdbRefreshJob"
  description: "Refresh TMDB metadata for movies and TV shows"

production_region:
  cron: "0 4 * * *"  # Every day at 4:00 AM (after import orchestrator)
  class: "ProductionRegionJob"
  description: "Compute production regions from country and language data"

wikidata_awards:
  cron: "0 5 * * 0"  # Every Sunday at 5:00 AM
  class: "WikidataAwardsJob"
  description: "Fetch film award winners from Wikidata"
